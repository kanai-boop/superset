# ユーザー体験（UX）要件

## 概要
マルチテナントSaaSとしてSupersetを提供する際に、お客さん（エンドユーザー）目線で必要な要素をまとめる。

---

## 1. オンボーディング体験

### 1-1. 初回ログイン時の体験

#### 必要な要素
- ✅ **ウェルカムメッセージ**: 初回ログイン時に歓迎メッセージを表示
- ✅ **チュートリアル**: 基本的な使い方をガイド
- ✅ **サンプルダッシュボード**: すぐに使えるサンプルを用意
- ✅ **クイックスタートガイド**: 5分で始められるガイド

#### 実装例
```python
# 初回ログイン時のチェック
def check_first_login(user_id: int) -> bool:
    """初回ログインかどうかをチェック"""
    last_login = get_user_last_login(user_id)
    return last_login is None

# ウェルカムダッシュボードを表示
def show_welcome_dashboard(user_id: int):
    """初回ログイン時にウェルカムダッシュボードを表示"""
    if check_first_login(user_id):
        # ウェルカムダッシュボードにリダイレクト
        return redirect("/dashboard/welcome")
```

### 1-2. オンボーディングフロー

```
1. アカウント作成完了メール
   ↓
2. 初回ログイン
   ↓
3. ウェルカムダッシュボード表示
   ↓
4. チュートリアル（オプション）
   ↓
5. サンプルダッシュボードで試す
   ↓
6. 自分のデータで使う
```

---

## 2. 使いやすさ・UX

### 2-1. ナビゲーション

#### 必要な要素
- ✅ **わかりやすいメニュー**: 何がどこにあるか明確
- ✅ **検索機能**: ダッシュボード・チャートを検索
- ✅ **お気に入り機能**: よく使うダッシュボードをお気に入りに
- ✅ **最近使ったもの**: 最近アクセスしたダッシュボードを表示

### 2-2. エラーメッセージ

#### 必要な要素
- ✅ **わかりやすいエラーメッセージ**: 技術的な用語を避ける
- ✅ **解決方法の提示**: エラーが起きた時の対処法を表示
- ✅ **サポートへのリンク**: 困った時にサポートに連絡できる

#### 実装例
```python
# ユーザーフレンドリーなエラーメッセージ
ERROR_MESSAGES = {
    "DATABASE_CONNECTION_ERROR": {
        "title": "データに接続できません",
        "message": "データベースへの接続に失敗しました。しばらく時間をおいて再度お試しください。",
        "solution": "問題が続く場合は、サポートまでお問い合わせください。",
        "support_link": "/support/contact"
    },
    "QUERY_TIMEOUT": {
        "title": "データの取得に時間がかかっています",
        "message": "データ量が多いため、表示に時間がかかっています。",
        "solution": "期間を短くするか、フィルタを追加してください。",
        "support_link": "/support/contact"
    }
}
```

### 2-3. ローディング状態

#### 必要な要素
- ✅ **ローディングインジケーター**: データを取得中であることを明確に表示
- ✅ **進捗表示**: どのくらい時間がかかるか表示
- ✅ **キャンセル機能**: 長時間かかる場合はキャンセルできる

---

## 3. ドキュメント・ヘルプ

### 3-1. ヘルプコンテンツ

#### 必要な要素
- ✅ **使い方ガイド**: 基本的な使い方を説明
- ✅ **よくある質問（FAQ）**: よくある質問と回答
- ✅ **動画チュートリアル**: 視覚的にわかりやすい動画
- ✅ **用語集**: 専門用語の説明

### 3-2. ヘルプの配置

#### 実装例
- **ヘルプボタン**: 画面右上に常に表示
- **コンテキストヘルプ**: 各画面で関連するヘルプを表示
- **ツールチップ**: アイコンやボタンにホバーで説明を表示

### 3-3. ドキュメントの内容

#### 必須ドキュメント
1. **はじめに**
   - ログイン方法
   - 基本的な画面の説明

2. **ダッシュボードの使い方**
   - ダッシュボードの見方
   - フィルタの使い方
   - 期間の変更方法

3. **チャートの作成方法**
   - チャートの作成手順
   - よく使うチャートタイプ
   - メトリクスの選び方

4. **よくある質問**
   - データが表示されない
   - エラーが発生する
   - パスワードを忘れた

---

## 4. サポート

### 4-1. サポートチャネル

#### 必要な要素
- ✅ **メールサポート**: 基本的なサポートチャネル
- ✅ **お問い合わせフォーム**: 簡単に問い合わせできる
- ✅ **チャットサポート（オプション）**: リアルタイムで対応

### 4-2. サポートの品質

#### 必要な要素
- ✅ **迅速な対応**: 24時間以内に返信（営業時間内）
- ✅ **わかりやすい回答**: 技術的な用語を避ける
- ✅ **解決までのフォロー**: 問題が解決するまで対応

### 4-3. サポート情報の収集

#### 実装例
```python
# お問い合わせフォーム
class SupportRequest:
    def __init__(self):
        self.tenant_id = get_current_tenant_id()
        self.user_id = get_current_user_id()
        self.dashboard_id = get_current_dashboard_id()
        self.error_message = get_last_error()
        self.browser_info = get_browser_info()
    
    def submit(self, message: str, category: str):
        """サポートリクエストを送信"""
        support_ticket = {
            "tenant_id": self.tenant_id,
            "user_id": self.user_id,
            "message": message,
            "category": category,
            "context": {
                "dashboard_id": self.dashboard_id,
                "error_message": self.error_message,
                "browser_info": self.browser_info
            },
            "timestamp": datetime.now()
        }
        
        # サポートシステムに送信
        send_to_support_system(support_ticket)
```

---

## 5. データの更新・新鮮度

### 5-1. データの更新頻度

#### 必要な要素
- ✅ **更新頻度の表示**: データがいつ更新されたか表示
- ✅ **自動更新**: 定期的にデータを更新
- ✅ **手動更新**: ユーザーが手動で更新できる

#### 実装例
```python
# データの更新時刻を表示
def get_data_freshness(dataset_id: int) -> Dict:
    """データの新鮮度を取得"""
    last_update = get_dataset_last_update(dataset_id)
    return {
        "last_update": last_update,
        "status": "fresh" if (datetime.now() - last_update).hours < 24 else "stale",
        "message": f"最終更新: {last_update.strftime('%Y年%m月%d日 %H:%M')}"
    }
```

### 5-2. データが古い場合の通知

#### 必要な要素
- ✅ **警告表示**: データが古い場合に警告を表示
- ✅ **更新予定時刻**: 次回更新予定時刻を表示

---

## 6. カスタマイズ性

### 6-1. ダッシュボードのカスタマイズ

#### 必要な要素
- ✅ **レイアウト変更**: チャートの配置を変更できる
- ✅ **色の変更**: チャートの色を変更できる
- ✅ **期間の設定**: デフォルトの期間を設定できる

### 6-2. 通知設定

#### 必要な要素
- ✅ **メール通知**: 重要な更新をメールで通知
- ✅ **通知設定**: 通知の種類を選択できる

---

## 7. モバイル対応

### 7-1. レスポンシブデザイン

#### 必要な要素
- ✅ **モバイル表示**: スマートフォンで見やすい
- ✅ **タッチ操作**: タッチ操作に対応
- ✅ **軽量表示**: モバイルでは軽量な表示

### 7-2. モバイルアプリ（将来）

#### 検討事項
- 📋 ネイティブアプリ（iOS/Android）
- 📋 PWA（Progressive Web App）

---

## 8. エクスポート・共有

### 8-1. データのエクスポート

#### 必要な要素
- ✅ **CSVエクスポート**: データをCSVでエクスポート
- ✅ **PDFエクスポート**: ダッシュボードをPDFでエクスポート
- ✅ **画像エクスポート**: チャートを画像でエクスポート

### 8-2. 共有機能

#### 必要な要素
- ✅ **リンク共有**: ダッシュボードのリンクを共有
- ✅ **埋め込み**: ダッシュボードを他のサイトに埋め込み
- ✅ **権限管理**: 共有先の権限を管理

---

## 9. パフォーマンス・速度

### 9-1. 表示速度

#### 必要な要素
- ✅ **高速表示**: ダッシュボードが3秒以内に表示される
- ✅ **段階的読み込み**: 重要なデータから順に表示
- ✅ **キャッシュ**: よく使うデータをキャッシュ

### 9-2. ユーザーへのフィードバック

#### 必要な要素
- ✅ **ローディング表示**: データ取得中であることを表示
- ✅ **進捗表示**: どのくらい時間がかかるか表示

---

## 10. アクセシビリティ

### 10-1. 基本的なアクセシビリティ

#### 必要な要素
- ✅ **キーボード操作**: キーボードだけで操作できる
- ✅ **スクリーンリーダー対応**: スクリーンリーダーで読み上げ可能
- ✅ **色のコントラスト**: 十分なコントラスト比

### 10-2. 多言語対応（将来）

#### 検討事項
- 📋 日本語・英語対応
- 📋 その他の言語（必要に応じて）

---

## 11. フィードバック収集

### 11-1. フィードバック方法

#### 必要な要素
- ✅ **フィードバックボタン**: 画面内にフィードバックボタン
- ✅ **アンケート**: 定期的にアンケートを実施
- ✅ **ユーザーインタビュー**: 定期的にユーザーインタビュー

### 11-2. フィードバックの活用

#### 実装例
```python
# フィードバック収集
class FeedbackCollector:
    def collect_feedback(
        self,
        user_id: int,
        feedback_type: str,
        message: str,
        rating: int = None
    ):
        """フィードバックを収集"""
        feedback = {
            "user_id": user_id,
            "tenant_id": get_tenant_id(user_id),
            "feedback_type": feedback_type,
            "message": message,
            "rating": rating,
            "timestamp": datetime.now()
        }
        
        # フィードバックを保存
        save_feedback(feedback)
        
        # 重要なフィードバックは通知
        if rating and rating <= 2:
            notify_support(feedback)
```

---

## 12. MVPでの優先順位

### 高優先度（MVPで必須）

1. ✅ **オンボーディング**
   - ウェルカムメッセージ
   - サンプルダッシュボード
   - 基本的な使い方ガイド

2. ✅ **エラーメッセージ**
   - わかりやすいエラーメッセージ
   - 解決方法の提示

3. ✅ **ヘルプ・ドキュメント**
   - 基本的な使い方ガイド
   - よくある質問（FAQ）

4. ✅ **サポート**
   - お問い合わせフォーム
   - メールサポート

### 中優先度（MVP後すぐに追加）

5. ⚠️ **データの更新頻度表示**
   - 最終更新時刻の表示

6. ⚠️ **エクスポート機能**
   - CSVエクスポート
   - PDFエクスポート

7. ⚠️ **検索機能**
   - ダッシュボード・チャートの検索

### 低優先度（将来対応）

8. 📋 **モバイル対応**
   - レスポンシブデザイン
   - モバイルアプリ

9. 📋 **カスタマイズ性**
   - レイアウト変更
   - 色の変更

10. 📋 **通知機能**
    - メール通知
    - アラート

---

## まとめ

### ユーザー目線で重要な要素

1. **わかりやすさ**: エラーメッセージ、ヘルプ、ドキュメント
2. **サポート**: 困った時に助けを求められる
3. **オンボーディング**: すぐに使い始められる
4. **パフォーマンス**: 快適に使える速度
5. **フィードバック**: ユーザーの声を聞く仕組み

### MVPでの実装方針

**「お客さんが迷わず使える」ことを最優先に**

技術的な完璧さよりも、お客さんが実際に使えるかどうかが重要です。まずは基本的な使いやすさを確保し、お客さんのフィードバックを得ながら改善していくことが大切です。

